// Efeito de mudança no header ao rolar a página
window.addEventListener('scroll', function() {
    const header = document.getElementById('header');
    if (window.scrollY > 50) {
        header.style.padding = '0.5rem 0';
        header.style.background = '#ffffff';
    } else {
        header.style.padding = '1rem 0';
        header.style.background = 'rgba(255, 255, 255, 0.95)';
    }
});

// Menu Mobile (Lógica básica)
const menuToggle = document.querySelector('.menu-toggle');
const navLinks = document.querySelector('.nav-links');

// Adicione aqui um listener se desejar implementar o menu mobile completo
// Para este exemplo, focamos na estrutura principal e responsividade visual.
