
// Efeito de scroll no Header
window.addEventListener('scroll', function() {
    const header = document.querySelector('#header');
    if (window.scrollY > 50) {
        header.style.padding = '10px 0';
        header.style.backgroundColor = '#ffffff';
    } else {
        header.style.padding = '20px 0';
    }
});

// Mensagem de boas-vindas no console (opcional)
console.log("Agro Forte, Futuro Sustentável - Site carregado com sucesso!");

// Aqui você poderia adicionar um menu mobile funcional
const menuToggle = document.querySelector('#mobile-menu');
const navLinks = document.querySelector('.nav-links');

if (menuToggle) {
    menuToggle.addEventListener('click', () => {
        // Logica para abrir/fechar menu em telas pequenas
        alert("O menu mobile seria ativado aqui!");
    });
}
